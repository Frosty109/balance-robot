The session is finished. Complete all end-of-session tasks in order:

1. **Session document** — create or update `sessions/YYYY-MM-DD.md` with what was done and what's next (follow the /session command instructions)

2. **Learning notes** — append any new concepts to the relevant files in `learning/` under a dated section (follow the /learning command instructions)

3. **Issues log** — create or append to `issues/YYYY-MM-DD.md` with any problems encountered (follow the /issues command instructions)

4. **Devlog capture** — only if `devlog/` exists in this project: capture any post-worthy material (follow the /devlog command instructions). Skip silently if the directory isn't there.

Do 1–3 even if a category seems thin — a short entry is better than a gap. Step 4 is allowed to produce nothing if the session had no post-worthy material; do not pad it.

Do not run /plan — plans are written on demand, not at session end.

Confirm when all are done.