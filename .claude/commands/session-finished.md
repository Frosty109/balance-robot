The session is finished. Complete all three end-of-session tasks in order:

1. **Session document** — create or update `session-notes/YYYY-MM-DD.md` with what was done and what's next (follow the /session command instructions)

2. **Learning notes** — append any new concepts to the relevant files in `Learning-Journal/` under a dated section (follow the /learning command instructions)

3. **Issues log** — create or append to `issues/YYYY-MM-DD.md` with any problems encountered (follow the /issues command instructions)

Do all three even if a category seems thin — a short entry is better than a gap. Confirm when all three are done.
